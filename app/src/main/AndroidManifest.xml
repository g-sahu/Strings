<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mediaplayer">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".strings.activities.SplashScreenActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".strings.activities.HomeActivity" />
        <activity
            android:name=".strings.activities.MediaPlayerActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask" />
        <activity android:name=".strings.activities.PlaylistActivity" />

        <service
            android:name=".strings.services.MediaPlayerService"
            android:exported="false" />
        <service
            android:name=".strings.services.MediaManagerService"
            android:exported="false" />

        <receiver
            android:name=".strings.services.MediaPlayerReceiver"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.mediaplayer.SERVICE_BROADCAST" />
            </intent-filter>
        </receiver>
    </application>

</manifest>